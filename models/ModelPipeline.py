class ModelPipeline:
      def __init__(self, pipeline, time, status, metrics):
            self.pipeline = pipeline
            self.time = time
            self.status = status
            self.metrics = metrics